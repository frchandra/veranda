version: '3.8'

services:
  veranda:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - internal
    environment:
      - NODE_ENV=production

networks:
  internal:
    driver: bridge
